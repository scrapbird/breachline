# AWS Region (default: ap-southeast-2)
aws_region = "ap-southeast-2"

# Note: Both Stripe API secret key (sk_...) and Stripe webhook secret (whsec_...)
# are stored in AWS Secrets Manager and must be configured separately after deployment.
# See README.md for details.
