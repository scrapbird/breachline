---
title: "Download"
date: 2025-01-01T00:00:00Z
---

# Download BreachLine

Every download is an archive. Extract it and run the application: there is no installer. See [Installation](/docs/getting-started/installation/) for step-by-step instructions.

## Windows
- [Download for Windows (64-bit)]() - `.zip` containing `BreachLine.exe`, for Windows 10/11

## macOS
- [Download for macOS (Intel)]() - `.tar.gz` containing `BreachLine.app`, for Intel-based Macs
- [Download for macOS (Apple Silicon)]() - `.tar.gz` containing `BreachLine.app`, for M1/M2/M3 Macs

## Linux
- [Download for Linux (x86_64)]() - `.tar.gz` containing the binary, for Ubuntu, Fedora, Arch, and other distributions
- [Download for Linux (ARM64)]() - `.tar.gz` containing the binary, for ARM-based systems
